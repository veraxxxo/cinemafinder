# Загрузка страниц, защищённых проверкой отпечатка клиента.
# Ставится только в GitHub Actions; без него сбор работает через браузер
# и открытые прокси, просто хуже пробивается.
scrapling>=0.3

# Браузерный движок для скрытого режима Scrapling. Без него `scrapling install`
# падает с «No module named playwright», ветка молча выключается на первом же
# запросе — ровно так и вышло 09.08: Scrapling не сделал ни одного вызова.
playwright>=1.44
